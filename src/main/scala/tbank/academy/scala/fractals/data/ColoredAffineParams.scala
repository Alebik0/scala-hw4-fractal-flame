package tbank.academy.scala.fractals.data

case class ColoredAffineParams(
                                params: AffineParams,
                                red: Int,
                                green: Int,
                                blue: Int
                              )
