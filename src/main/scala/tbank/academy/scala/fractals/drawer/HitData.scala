package tbank.academy.scala.fractals.drawer

case class HitData(imageX: Int,
                           imageY: Int,
                           red: Int,
                           green: Int,
                           blue: Int)
